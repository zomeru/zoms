import blockContent from './blockContent';
import blogPost from './blogPost';
import experience from './experience';
import project from './project';

export const schemaTypes = [blockContent, blogPost, experience, project];
